# Определите базовый образ. Используйте официальный образ Node.js
FROM node:20

# Установите рабочий каталог в контейнере
WORKDIR /usr/src/app

# Копируйте файлы package.json и package-lock.json
COPY package*.json ./

# Установите зависимости проекта
RUN npm install

# Копируйте исходный код проекта в рабочий каталог контейнера
COPY . .

# Определите порт, который будет доступен снаружи контейнера
EXPOSE 3000

# Запускайте ваше приложение при старте контейнера
CMD ["node", "server.js"]
